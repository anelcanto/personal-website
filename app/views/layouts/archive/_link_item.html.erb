 common_items = {
      root_path: "Home",
      blog_posts_path: "Blog"
  }
  common_items_class = "font-medium text-gray-500 hover:text-gray-400 md:py-6 dark:text-gray-400 dark:hover:text-gray-500"
      
      <% common_items.map do |path, text| %>
        <%= link_to text, path: path, class: common_items_class %>
      <% end %>
      <%= link_to path: root_path, class: "font-medium text-gray-500 hover:text-gray-400 md:py-6 dark:text-gray-400 dark:hover:text-gray-500"%>
      


        
          <% if user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path,data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
          <%else%>
            <%= link_to "Log in",  new_user_session_path %>
          <%end%>